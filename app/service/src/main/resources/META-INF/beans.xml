<?xml version="1.0" encoding="UTF-8"?>
<!--
    CDI bean discovery marker for multi-module Maven projects.
    
    Enables CDI to scan this module for @ApplicationScoped, @Singleton,
    and other bean-defining annotations. Required because each Maven
    module is a separate archive that needs explicit bean discovery.
    
    Annotations define WHAT is a bean; beans.xml tells CDI WHERE to look.
-->
<beans xmlns="https://jakarta.ee/xml/ns/jakartaee"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                           https://jakarta.ee/xml/ns/jakartaee/beans_4_0.xsd"
       version="4.0"
       bean-discovery-mode="annotated">
</beans>